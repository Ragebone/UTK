[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["UtkBase", "UtkCommon", "UtkAmd"]

[project]
name = "Rage UTK"
version = "0.0.1"
description = "My package description"
readme = "README.rst"
requires-python = ">=3.8"
dependencies = [
]